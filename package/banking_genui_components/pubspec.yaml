name: banking_genui_components
description: Banking UI components for GenUI/A2UI integration - provides a widget catalog for AI-driven banking interfaces.
version: 0.0.1

environment:
  sdk: ^3.10.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.8.0
  json_schema_builder: ^0.1.3
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
